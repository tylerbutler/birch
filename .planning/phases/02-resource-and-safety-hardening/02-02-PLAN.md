---
phase: 02-resource-and-safety-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/birch/internal/async_actor.gleam
  - src/birch/handler/async.gleam
autonomous: true
requirements:
  - RES-03
  - RES-04
  - RES-05

must_haves:
  truths:
    - "Async actor tracks queue length as O(1) integer field"
    - "Async actor uses Erlang :queue via FFI for DropOldest"
    - "Async actor detects handler crash and logs failure"
  artifacts:
    - path: "src/birch/internal/async_actor.gleam"
      contains: "queue_len:"
      provides: "O(1) queue length tracking"
    - path: "src/birch/internal/async_actor.gleam"
      contains: "DOWN"
      provides: "Process crash detection"
  key_links:
    - from: "handler/async.gleam"
      to: "internal/async_actor.gleam"
      via: "actor.start call"
      pattern: "async_actor.start"
---

<objective>
Improve async actor with O(1) queue tracking, proper queue data structure, and crash monitoring.

Purpose: Eliminate O(n) list.length calls, enable efficient DropOldest, detect handler crashes.
Output: Async actor with queue_len field, :queue usage, and process monitoring.
</objective>

<context>
@src/birch/internal/async_actor.gleam
@src/birch/handler/async.gleam

Current state:
- async_actor.gleam uses List(LogRecord) for pending queue
- Line 133 uses list.length(state.pending) >= state.max_queue_size (O(n))
- DropOldest uses list.take which is inefficient
- No process monitoring for handler crashes

FFI already has :queue via birch_ffi.erl - need to use it from Gleam
</context>

<tasks>

<task type="auto">
  <name>RES-03 + RES-04: Add queue_len field and use :queue via FFI</name>
  <files>src/birch/internal/async_actor.gleam</files>
  <action>
Add O(1) queue length tracking and replace list with Erlang :queue.

1. Add queue_len: Int field to State record (after pending field)
2. Add FFI declarations for Erlang :queue operations:
   - queue_new() -> Queue(a)
   - queue_in(item, queue) -> Queue(a)
   - queue_out(queue) -> Result(#(a, Queue(a)), Nil)
   - queue_is_empty(queue) -> Bool
3. Replace pending: List(LogRecord) with pending: Queue(LogRecord)
4. Replace list.length(state.pending) >= state.max_queue_size with state.queue_len >= state.max_queue_size
5. Update handle_message to:
   - On Log: increment queue_len on enqueue
   - On flush: reset queue_len to 0
6. Update handle_overflow to use :queue operations:
   - DropOldest: use queue:out to remove from front, queue:in to add to back
   - DropNewest: just don't enqueue
   - Block: flush then enqueue
7. Update flush_pending to work with :queue

Initial state should initialize: pending = queue_new(), queue_len = 0
</action>
  <verify>
just test-erlang
  </verify>
  <done>Tests pass; queue_len field used instead of list.length</done>
</task>

<task type="auto">
  <name>RES-05: Add process monitoring for crash detection</name>
  <files>src/birch/internal/async_actor.gleam</files>
  <action>
Add erlang:monitor/2 to detect when wrapped handler process crashes.

1. Add monitor_ref: String field to State record to track the monitor reference
2. Add FFI for starting handler with monitoring:
   - spawn_handler_with_monitor(Handler) -> #(Pid, MonitorRef)
3. In handle_message, handle DOWN messages:
   - Receive {'DOWN', Ref, process, Pid, Reason} 
   - Log error about handler crash
   - Either stop actor or restart handler
4. Update start/3 to use spawn_handler_with_monitor
5. Handle monitor reference in state

For crash recovery: log error and stop (consistent with startup behavior - fall back to sync)
</action>
  <verify>
just test-erlang
  </verify>
  <done>Tests pass; DOWN messages handled in actor</done>
</task>

</tasks>

<verification>
All Erlang tests pass. Async handler handles queue overflow and monitors correctly.
</verification>

<success_criteria>
- queue_len field tracks length as O(1) integer
- :queue module used for queue operations
- Process monitoring catches handler crashes
- All tests pass on Erlang target
</success_criteria>

<output>
After completion, create `.planning/phases/02-resource-and-safety-hardening/02-02-SUMMARY.md`
</output>
