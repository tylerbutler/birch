---
phase: 02-resource-and-safety-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/birch/handler/file.gleam
autonomous: true
requirements:
  - RES-06

must_haves:
  truths:
    - "File handler caches file size in memory"
    - "stat() only called during rotation checks"
  artifacts:
    - path: "src/birch/handler/file.gleam"
      contains: "cached_size:"
      provides: "In-memory file size cache"
  key_links:
    - from: "handler/file.gleam"
      to: "FileConfig"
      via: "write_to_file updates cache"
      pattern: "cached_size"
---

<objective>
Cache file size in memory to avoid stat() on every write.

Purpose: Eliminate expensive disk I/O on every log write; only stat during rotation checks.
Output: FileConfig with cached_size field, updated on write.
</objective>

<context>
@src/birch/handler/file.gleam

Current state:
- should_rotate_by_size calls file_size(path) which calls simplifile.file_info every time
- This does disk I/O on every log write
- After rotation, file is new so size should reset to 0
</context>

<tasks>

<task type="auto">
  <name>RES-06: Cache file size in FileConfig</name>
  <files>src/birch/handler/file.gleam</files>
  <action>
Add cached_size field to FileConfig and update on each write.

1. Add cached_size: Int field to FileConfig type (default to 0)
2. Modify write_to_file to:
   - Take FileConfig as mutable (need to pass updated config back)
   - After successful append, increment cached_size by message length
   - Only call file_size during rotation check, not on every write
3. Modify should_rotate_by_size to:
   - Accept cached_size parameter instead of calling file_size
   - Call file_size only if we don't have cached value
4. After rotation (rotate_file, rotate_file_with_timestamp):
   - Reset cached_size to 0 (new file = empty)
5. Update handler/handler_with_formatter to:
   - Initialize cached_size to 0
   - Pass updated config through write closure (use let mutable or ref cell pattern)

Note: The write function needs to return updated config for the cache to persist.
One approach: Store config in a dict or use a process dictionary key for the file handler.
Alternative: Make the write function accept config ref that gets updated.

Simpler approach: Use process dictionary via FFI to store per-handler cached sizes.
</action>
  <verify>
just test-erlang
  </verify>
  <done>Tests pass; file size cached between writes</done>
</task>

</tasks>

<verification>
All Erlang tests pass. File handler caches size correctly and resets on rotation.
</verification>

<success_criteria>
- cached_size field in FileConfig
- file_size() only called during rotation checks
- cached_size resets to 0 after rotation
- All tests pass on Erlang target
</success_criteria>

<output>
After completion, create `.planning/phases/02-resource-and-safety-hardening/02-03-SUMMARY.md`
</output>
