---
phase: 02-resource-and-safety-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/birch/config.gleam
  - src/birch_ffi.erl
autonomous: true
requirements:
  - RES-01
  - RES-02

must_haves:
  truths:
    - "configure() and set_level() write to a single persistent_term key"
    - "Async writer registry uses ETS table instead of persistent_term"
  artifacts:
    - path: "src/birch_ffi.erl"
      contains: "ets:new(birch_async_registry"
    - path: "src/birch_ffi.erl"
      contains: "birch_state"
      provides: "Single key for config+logger"
  key_links:
    - from: "src/birch/config.gleam"
      to: "src/birch_ffi.erl"
      via: "set_global_config FFI call"
      pattern: "set_global_config"
---

<objective>
Consolidate persistent_term keys and move async registry to ETS.

Purpose: Reduce GC passes from 2 to 1 for config changes, eliminate GC on registry updates.
Output: Single persistent_term key for config+logger, ETS-based async registry.
</objective>

<context>
@src/birch/config.gleam
@src/birch_ffi.erl

Current state:
- birch_ffi.erl uses 2 separate keys: birch_global_config and birch_cached_default_logger
- Async registry uses persistent_term at birch_async_registry (line 126)
- Every put/erase to persistent_term triggers global GC
</context>

<tasks>

<task type="auto">
  <name>RES-01: Consolidate to single persistent_term key</name>
  <files>src/birch_ffi.erl, src/birch/config.gleam</files>
  <action>
Replace the two separate persistent_term keys (birch_global_config, birch_cached_default_logger) with a single key "birch_state" containing a tuple {Config, Logger}.

In birch_ffi.erl:
1. Replace -define(GLOBAL_CONFIG_KEY, birch_global_config). with -define(STATE_KEY, birch_state).
2. Replace get_global_config to read from STATE_KEY tuple: persistent_term:get(?STATE_KEY) returns {Config, Logger}, extract Config.
3. Replace set_global_config to store both: persistent_term:put(?STATE_KEY, {Config, Logger}).
4. Add get_cached_default_logger that extracts Logger from the tuple.
5. Add set_cached_default_logger that updates only the Logger part of the tuple.
6. Add clear functions that erase the single key.
7. Remove the old CACHED_LOGGER_KEY define.

In config.gleam:
1. Update FFI declarations to match new function names.
</action>
  <verify>
just test-erlang
  </verify>
  <done>Tests pass; persistent_term writes use single key</done>
</task>

<task type="auto">
  <name>RES-02: Move async registry to ETS table</name>
  <files>src/birch_ffi.erl</files>
  <action>
Replace persistent_term-based async registry with ETS table to avoid GC on every write.

In birch_ffi.erl, modify registry functions:
1. Create ETS table on first use: ets:new(birch_async_registry, [set, named_table, public])
2. register_writer/2: Replace persistent_term:put with ets:insert(birch_async_registry, {Name, Pid})
3. unregister_writer/0: Replace persistent_term:put with ets:delete(birch_async_registry, self()) or by name
4. get_all_writers/0: Replace persistent_term:get with ets:tab2list(birch_async_registry)
5. get_writer/1: Use ets:lookup instead of lists:keyfind
6. Keep the ?ASYNC_REGISTRY_KEY constant but change its usage from persistent_term to ETS

Note: The ETS table is created on first access (lazy initialization).
</action>
  <verify>
just test-erlang
  </verify>
  <done>Tests pass; async registry uses ETS table</done>
</task>

</tasks>

<verification>
All Erlang tests pass. Async handlers work correctly with new registry.
</verification>

<success_criteria>
- Single persistent_term key used for config changes
- ETS table used for async registry updates
- All tests pass on Erlang target
</success_criteria>

<output>
After completion, create `.planning/phases/02-resource-and-safety-hardening/02-01-SUMMARY.md`
</output>
