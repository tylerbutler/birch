---
phase: 03-api-surface-and-idiomatic-gleam-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/birch/logger.gleam
  - src/birch/handler.gleam
  - src/birch/record.gleam
  - src/birch/level_formatter.gleam
  - test/birch_test.gleam
  - test/property_test.gleam
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "All type-level getters use bare names (level, handlers, context, etc.) -- no get_ prefix"
    - "Module-level global state getters in birch.gleam keep get_ prefix (get_level, get_config, etc.)"
    - "Config module getters keep get_ prefix due to ConfigOption constructor naming conflict"
    - "All tests pass on both Erlang and JavaScript targets after renames"
  artifacts:
    - path: "src/birch/logger.gleam"
      provides: "Renamed getters: level(), handlers(), context()"
      contains: "pub fn level(logger: Logger)"
    - path: "src/birch/handler.gleam"
      provides: "Renamed getter: error_callback()"
      contains: "pub fn error_callback(handler: Handler)"
    - path: "src/birch/record.gleam"
      provides: "Renamed getters: metadata(), caller_id()"
      contains: "pub fn metadata("
    - path: "src/birch/level_formatter.gleam"
      provides: "Renamed getter: target_width()"
      contains: "pub fn target_width(formatter: LevelFormatter)"
  key_links:
    - from: "test/birch_test.gleam"
      to: "src/birch/logger.gleam"
      via: "test calls to renamed getters"
      pattern: "logger\\.level\\(|logger\\.handlers\\(|logger\\.context\\("
    - from: "test/property_test.gleam"
      to: "src/birch/record.gleam"
      via: "property test calls to renamed getters"
      pattern: "record\\.metadata\\(|record\\.caller_id\\("
---

<objective>
Rename all type-level `get_*` accessor functions to bare getter style across logger, handler, record, and level_formatter modules. Update all test files and examples to use the new names.

Purpose: Establish consistent idiomatic Gleam naming (bare getters like gleam_stdlib) before 1.0 stabilization.
Output: All `get_*` type accessors renamed to bare form; all tests and examples updated and passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-api-surface-and-idiomatic-gleam-audit/03-CONTEXT.md
@.planning/phases/03-api-surface-and-idiomatic-gleam-audit/03-RESEARCH.md

<interfaces>
<!-- Renames to perform (Current -> New) -->

From src/birch/logger.gleam:
  get_level(logger) -> level(logger)
  get_handlers(logger) -> handlers(logger)
  get_context(logger) -> context(logger)

From src/birch/handler.gleam:
  get_error_callback(handler) -> error_callback(handler)

From src/birch/record.gleam:
  get_metadata(record, key) -> metadata(record, key)
  get_caller_id(record) -> caller_id(record)

From src/birch/level_formatter.gleam:
  get_target_width(formatter) -> target_width(formatter)

KEEP AS-IS (module-level global state getters in birch.gleam):
  get_level(), get_config(), get_scoped_logger(), get_scope_context()

KEEP AS-IS (config.gleam -- naming conflict with ConfigOption constructors):
  get_level(config), get_on_error(config)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename get_* to bare getters in source modules</name>
  <files>src/birch/logger.gleam, src/birch/handler.gleam, src/birch/record.gleam, src/birch/level_formatter.gleam</files>
  <action>
Rename the following public functions (change function name only, keep the body and doc comments, update doc comments to match new name):

1. **logger.gleam**: `get_level` -> `level`, `get_handlers` -> `handlers`, `get_context` -> `context`
   - Note: `name()` already exists as bare getter -- no change needed there.

2. **handler.gleam**: `get_error_callback` -> `error_callback`
   - Note: `name()` and `should_handle()` already correct -- no change.

3. **record.gleam**: `get_metadata` -> `metadata`, `get_caller_id` -> `caller_id`
   - `metadata(record, key)` takes two args (record + key lookup). This is fine as bare name.

4. **level_formatter.gleam**: `get_target_width` -> `target_width`

Do NOT add deprecated aliases -- pre-1.0, direct rename is appropriate per user decision.
Do NOT touch birch.gleam module-level getters (get_level, get_config, etc.) -- those are global state getters, not type accessors.
Do NOT touch config.gleam getters (get_level, get_on_error) -- naming conflict with ConfigOption constructors.

After renaming, check for any internal callers within the same source files that reference the old names and update them.
  </action>
  <verify>
    <automated>cd /home/tylerbu/code/claude-workspace/birch && gleam build 2>&1 | head -50</automated>
  </verify>
  <done>All four modules have bare getter names. gleam build produces errors only in test/example files (expected -- updated in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Update all test files, examples, and internal callers to use new getter names</name>
  <files>test/birch_test.gleam, test/property_test.gleam, test/erlang_logger_test.gleam, examples/</files>
  <action>
Find and replace all references to the old getter names across the entire codebase (excluding the already-renamed source modules):

1. `logger.get_level(` -> `logger.level(`  (or `log.get_level(` depending on import alias)
2. `logger.get_handlers(` -> `logger.handlers(`
3. `logger.get_context(` -> `logger.context(`
4. `handler.get_error_callback(` -> `handler.error_callback(`
5. `record.get_metadata(` -> `record.metadata(`
6. `record.get_caller_id(` -> `record.caller_id(`
7. `level_formatter.get_target_width(` -> `level_formatter.target_width(`

Search locations:
- `test/birch_test.gleam` -- main test file with ~20+ references
- `test/property_test.gleam` -- property tests with ~8 references
- `test/erlang_logger_test.gleam` -- OTP tests
- All 17 example directories in `examples/`
- Any other source files in `src/` that call these getters (check `src/birch.gleam`, `src/birch/formatter.gleam`, etc.)

Use `rg 'get_level\(|get_handlers\(|get_context\(|get_error_callback\(|get_metadata\(|get_caller_id\(|get_target_width\(' src/ test/ examples/` to find all call sites. Be careful NOT to rename `config.get_level` or `config.get_on_error` or birch.gleam's module-level `get_level()`, `get_config()`, `get_scoped_logger()`, `get_scope_context()`.

After all replacements, run `gleam format src test examples` to ensure formatting is clean.
  </action>
  <verify>
    <automated>cd /home/tylerbu/code/claude-workspace/birch && just test</automated>
  </verify>
  <done>All tests pass on both Erlang and JavaScript targets. No references to old get_* names remain in test/example files (except config.get_level, config.get_on_error, and birch.gleam module-level getters which are intentionally kept).</done>
</task>

</tasks>

<verification>
- `gleam build` succeeds with no errors
- `just test` passes on both Erlang and JavaScript targets
- `rg 'logger\.get_level|logger\.get_handlers|logger\.get_context|handler\.get_error_callback|record\.get_metadata|record\.get_caller_id|level_formatter\.get_target_width' src/ test/ examples/` returns no matches
- `rg 'config\.get_level|config\.get_on_error|birch\.get_level|birch\.get_config' src/` still has matches (these are intentionally kept)
</verification>

<success_criteria>
All type-level getter functions use bare names. All tests pass on both targets. No old get_* references remain except the documented exceptions (config module, birch.gleam module-level getters).
</success_criteria>

<output>
After completion, create `.planning/phases/03-api-surface-and-idiomatic-gleam-audit/03-01-SUMMARY.md`
</output>
