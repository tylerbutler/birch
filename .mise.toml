# mise configuration for gleam_log
# https://mise.jdx.dev/

# Use .tool-versions as the source of truth for versions
# This allows compatibility with setup-beam in CI

[tools]
erlang = "27.2.1"
gleam = "1.14.0"
just = "1.38.0"
node = "22"  # For JavaScript target testing

[env]
# Ensure consistent locale for tests
LC_ALL = "en_US.UTF-8"
