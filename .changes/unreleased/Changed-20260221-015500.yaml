kind: Changed
body: |-
    Change `GlobalConfig.sampling` field type from `Result(SampleConfig, Nil)` to `Option(SampleConfig)`.

    Migrate from `Ok(config)` / `Error(Nil)` to `Some(config)` / `None`.
time: 2026-02-21T01:55:00.000000000Z
