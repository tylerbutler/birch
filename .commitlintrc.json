{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["feat", "fix", "perf", "refactor", "docs", "chore", "build", "test", "ci", "style"]
    ],
    "subject-case": [2, "always", "lower-case"]
  }
}
